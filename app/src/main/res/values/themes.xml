<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TurnosPrincipal" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/cafePrincipal</item>
        <item name="colorPrimaryVariant">@color/cafePrincipal</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/cafePrincipal</item>
        <item name="colorSecondaryVariant">@color/cafePrincipal</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="BottomNavigation">
        <item name="android:background">@color/green</item>
        <item name="itemIconTint">@color/white</item>
        <item name="itemTextColor">@color/white</item>
    </style>

    <style name="CustomBottomNavIndicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@color/cafePrincipal</item>
    </style>

    <style name="Theme.TurnosPrincipal" parent="Base.Theme.TurnosPrincipal" />
</resources>